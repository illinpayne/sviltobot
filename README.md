# 💡 Telegram Bot "Світло" (Svitlobot)

## ⚡️ Опис проєкту
Telegram Bot "Світло" — це високоспеціалізований сервіс для **оперативного інформування користувачів про графіки відключень електроенергії (ГОП/ГАВ)**. Бот спрощує процес моніторингу, перетворюючи складні табличні дані з вебсайтів енергопостачальних компаній (ОСР) у прості та зрозумілі повідомлення Telegram.

### Ключові цілі
1.  **Актуальність:** Забезпечення користувача даними в режимі, максимально наближеному до реального часу.
2.  **Зручність:** Надання інформації про чергу та графік для конкретної адреси в мінімальну кількість кліків.
3.  **Автоматизація:** Мінімізація ручного моніторингу офіційних джерел користувачем.

---

## 🛠️ Технологічний стек
Проєкт розроблено на Python, використовуючи наступні ключові компоненти:

| Компонент | Технологія / Бібліотека                   | Призначення |
| :--- |:------------------------------------------| :--- |
| **Backend/Core** | Python 3.10+                              | Основна мова програмування. |
| **Telegram API** | `python-telegram-bot` (або `aiogram`)     | Обробка вхідних повідомлень та надсилання відповідей. |
| **Парсинг даних** | `Scrapy`                                  | Збір даних графіків з вебсайтів обленерго (OСР). |
| **База даних** | `SQLite`                                  | Зберігання даних користувачів (адреса, черга) та логування змін графіків. |

---

## 🏗️ Архітектура
Бот побудований за принципом **Модульного Сервісу** з чітким розділенням відповідальності:

1.  **Core (Ядро):** Містить логіку взаємодії з Telegram API, обробку команд (`/start`, `/address`, `/schedule`).
2.  **Data Scraper (Парсер):** Окремий модуль, відповідальний за:
    * Регулярне опитування (polling) офіційних джерел (вебсайтів).
    * Екстракцію (витягування) даних графіків та черг.
    * Нормалізацію та валідацію отриманих даних.
3.  **Database Layer (Шар БД):** Містить моделі та методи для CRUD-операцій з базою даних (зберігання прив'язки **User ID ↔️ Address ↔️ Queue ID**).
4.  **Notification Service (Сервіс сповіщень):** Фоновий процес, який порівнює поточний графік у БД з щойно отриманим. Якщо виявлено зміну (наприклад, перехід з планового на аварійне відключення), він розсилає сповіщення підписникам.